# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2018"
name = "settings-schema"
version = "0.2.0"
authors = ["Riccardo Zaglia <riccardo.zaglia5@gmail.com>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Derive macro for structs and enums GUI metadata"
readme = "../README.md"
keywords = [
    "settings",
    "schema",
]
categories = [
    "config",
    "data-structures",
    "gui",
]
license = "MIT"
repository = "https://github.com/zarik5/settings-schema-rs"

[lib]
name = "settings_schema"
path = "src/lib.rs"

[[example]]
name = "example"
path = "examples/example.rs"

[dependencies]
serde_json = "1"

[dependencies.serde]
version = "1"
features = ["derive"]

[dependencies.settings-schema-derive]
version = "0.2.0"
path = "../settings-schema-derive"
